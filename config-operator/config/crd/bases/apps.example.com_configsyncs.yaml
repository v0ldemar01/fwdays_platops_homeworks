---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: configsyncs.apps.example.com
spec:
  group: apps.example.com
  names:
    kind: ConfigSync
    listKind: ConfigSyncList
    plural: configsyncs
    singular: configsync
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ConfigSync is the Schema for the configsyncs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConfigSyncSpec defines the desired state of ConfigSync
            properties:
              configMapName:
                description: ConfigMapName is the name of the ConfigMap to create
                  or update
                type: string
              data:
                additionalProperties:
                  type: string
                description: Data contains key-value pairs that will be stored in
                  the ConfigMap
                type: object
              updateInterval:
                description: UpdateInterval specifies how often the controller should
                  check for changes (in seconds)
                type: integer
            required:
            - configMapName
            type: object
          status:
            description: ConfigSyncStatus defines the observed state of ConfigSync
            properties:
              lastSyncTime:
                description: LastSyncTime is the last time the ConfigMap was synced
                format: date-time
                type: string
              status:
                description: Status indicates the current sync status
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
